extends layout

block content
  h1= title
  p Welcome to #{title}
  p #{conta}
  each pista in pistas
    .col-xs-12.list-group-item
      h4
        p= pista.nome
      each foto in pista.fotos
          span.label.label-warning
            img(src= foto) 
          | &nbsp
